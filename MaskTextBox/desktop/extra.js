(function(f) {
    var c = ["enable", "disable", "flushCache", "trigger", "display", "close"],
        b = this,
        d = "autocompletecustom";

    f.fn.Roda = function() {
        bizagi.log ('string trace xxxxx' );
        return "teste";
    }
}(jQuery));